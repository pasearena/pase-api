{
  "name": "pase-arena-api",
  "version": "1.0.0",
  "description": "Pase Arena fight engine + leaderboard + waitlist API",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "node --import tsx --env-file=.env src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/rate-limit": "^9.1.0",
    "@supabase/supabase-js": "^2.45.4",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=20"
  }
}
